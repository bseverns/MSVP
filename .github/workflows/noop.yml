name: No-op CI

on:
  push:
  pull_request:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Echo info
        run: |
          echo "This is a placeholder workflow for the videoProcessing-midi-sync repo."
          echo "Processing sketches are not automatically built here."
